{
  "aliases": {
    "quick": "haiku"
  },
  "permissions": {
    "defaultMode": "default",
    "deny": [
      "bash(curl:*)",
      "bash(wget:*)",
      "bash(sudo:*)",
      "bash(rm:*)"
    ],
    "ask": [
      "bash"
    ]
  }
}
